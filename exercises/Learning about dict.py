"""examples of dictionary syntax with icecream shop order tallies."""

#Ice_cream: dict[str, int] = {
#    "chocolate": 12,
# /3    "vanilla": 8,
#    "strawberry": 4,
#}
# name: dict[<key type>, <vlue type>]


def max(input: list[int]) -> int:
    # Step 1: Check if the length of the list `input` is 0. If it is, raise a ValueError with exactly this message: "max() arg is an empty List".
    if len(input) == 0:
        print (f"{max()} arg is an empty List".)
# Step 2: Create a variable to track the largest number. Initialize it with the very first item in the `input` list.
    for num in list:
        if num != target:
            return False
# Step 3: Loop through every number in the `input` list.
# Step 4: Inside the loop, check if the current number is greater than your tracker variable. If it is, update the tracker to equal this current number.
# Step 5: After the loop finishes checking every number, return the tracker variable.